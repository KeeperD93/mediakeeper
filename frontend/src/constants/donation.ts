// MediaKeeper project support links shown on the donation panel (admin only).
// ``sponsor`` stays empty until GitHub Sponsors is approved — its row is
// hidden while blank. Distinct from the operator's own donation link, which
// each instance configures in the portal admin settings.
export const MEDIAKEEPER_SUPPORT = Object.freeze({
  kofi: 'https://ko-fi.com/keeperd93',
  repo: 'https://github.com/keeperd93/mediakeeper',
  sponsor: '',
})
