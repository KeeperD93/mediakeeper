# Runtime dependencies — installed into the production image (Dockerfile).
# Test-only tooling (pytest, aiosqlite) lives in requirements-dev.txt and is
# installed in CI only, never shipped in the public image.
fastapi==0.141.1
uvicorn[standard]==0.52.4
sqlalchemy==2.0.52
asyncpg==0.31.0
pydantic==2.13.5
httpx==0.28.1
python-dotenv==1.2.3
psutil==5.9.8
bcrypt==5.0.0
PyJWT[crypto]==2.13.0
python-multipart==0.0.32
aiofiles==25.1.0
alembic==1.19.1
slowapi==0.1.10
chardet==5.2.0
cryptography==50.0.1
bleach[css]==6.4.0
cachetools==7.1.8
